# 🏛️ AI 협력 개발 헌법 및 프롬프트 품질 보증 규칙 (AI_DEV_RULES)

이 문서는 안티그래비티와 개발 어시스턴트가 프로젝트를 진행할 때 절대 위반할 수 없는 영구 불문율이다.

1. 제1원칙: 하드코딩 절대 금지 (Zero-Hardcoding)
   - 프롬프트나 지시문에 포함된 예시 도메인 명칭(예: "심야 소음 다발", "천장 누수", "특정 파일명", "민원조치일지.xlsx 기반" 등)을 소스코드(TypeScript/React 컴포넌트)에 상수로 하드코딩하지 않는다.
   - 모든 대시보드 타이틀, KPI 카드, 데이터베이스 블록은 백엔드 AI가 분석하여 전달한 동적 페이로드(Payload/Variable)만을 참조하여 조립한다.

2. 제2원칙: 은폐형 가짜(Mock) 우회 금지 (Fail-Fast & Honest)
   - 파일 파싱 실패, 데이터 추출 실패 시 사전에 준비된 가짜 템플릿(Fallback Mock)을 몰래 띄우는 기만적 로직을 전면 금지한다.
   - 데이터가 없을 때는 즉시 사용자에게 실패 원인을 정직하게 고지하고, 캔버스는 빈 화면(null)으로 유지한다.

3. 제3원칙: 단일 책임 원칙 (Atomic Task Execution)
   - 한 번의 수정 지시에는 반드시 한 가지 기능/결함만 수정하고 빌드 검증을 거친다. 여러 컴포넌트를 한꺼번에 뜯어고쳐 사이드 이펙트를 유발하지 않는다.

4. 제4원칙: 파일 포맷 정직 대응 (Honest Parser Policy)
   - 바이너리 HWP(.hwp)는 웹 브라우저 단독 디코딩이 불가능하므로, 시스템 크래시를 방지하고 "PDF 또는 워드(DOCX)로 변환 권장" 안내를 명확히 출력한다.
   - HTML 테이블 형태의 .xls는 DOMParser로 안전 파싱하고, 순수 바이너리는 SheetJS(xlsx) ESM 방어 바인딩을 적용한다.

5. 제5원칙: 커밋 전 자체 검수 의무화 (Self-Diff Code Review)
   - git push 전에 반드시 `git diff`를 실행하여 예시 문자열 하드코딩 여부, 레거시 Mock 잔재 여부, TypeScript 컴파일 검증을 마친 후 푸시한다.
