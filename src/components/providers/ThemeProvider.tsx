export { 
  SparkThemeProvider as ThemeProvider, 
  UniversalThemeProvider, 
  useSparkTheme, 
  useTheme, 
  THEME_CONFIGS 
} from '../../context/SparkThemeContext';
export type { SparkThemeClasses, SparkTheme } from '../../context/SparkThemeContext';
